// Codefrogs easy-cpp
// Comments always start with '//'

/* Multi line comments are within 
   these forward slash and star markers.
   One more line here. But you can have more! */
 
#include <iostream>  // Declares a bunch of things that we might use.
                     // In this case 'cout', and 'endl'

int main()  // The start of any program.
{
    // Reads, using the function 'cout', print out the text
    // 'Hello world!'
    // where 'endl' says write out a new line
 
    std::cout << "Hello world!" << std::endl; 
   
    return 0; // return 0 to indicate the program worked OK.
}
